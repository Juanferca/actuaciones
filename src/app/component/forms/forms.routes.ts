import {RouterModule, Routes} from '@angular/router';
import {ModuleWithProviders} from '@angular/core';


const formRoutes: Routes = [];

export const formsRoutes: ModuleWithProviders = RouterModule.forRoot(formRoutes);